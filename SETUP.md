# Инструкция по настройке и запуску

## 🚀 Быстрый старт

### 1. Требования

- **macOS**: 13.0 (Ventura) или новее
- **Xcode**: 15.0 или новее
- **iOS Simulator**: iOS 15.0+
- **Устройство**: iPhone с iOS 15.0+ (опционально)

### 2. Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd monobank

# Откройте проект в Xcode
cd MonobankClone
open MonobankClone.xcodeproj
```

### 3. Первый запуск

1. В Xcode выберите симулятор (например, iPhone 15 Pro)
2. Нажмите **⌘R** или кнопку **Run**
3. Приложение запустится в симуляторе

## 📱 Тестирование на устройстве

### Настройка подписи кода

1. Откройте проект в Xcode
2. Выберите target **MonobankClone**
3. Перейдите в **Signing & Capabilities**
4. Выберите вашу **Team**
5. Измените **Bundle Identifier** на уникальный (например, `com.yourname.monobank`)

### Запуск на устройстве

1. Подключите iPhone через USB
2. Разблокируйте устройство
3. Выберите ваше устройство в Xcode
4. Нажмите **⌘R**
5. При первом запуске: **Settings → General → Device Management → Trust Developer**

## 🔧 Настройка Codemagic

### 1. Создание аккаунта

1. Перейдите на [codemagic.io](https://codemagic.io)
2. Зарегистрируйтесь через GitHub/GitLab/Bitbucket
3. Подключите репозиторий

### 2. Настройка App Store Connect

1. В Codemagic перейдите в **Teams → Integrations**
2. Добавьте **App Store Connect**
3. Введите:
   - Issuer ID
   - Key ID
   - Private Key (.p8 file)

### 3. Настройка сертификатов

1. В Codemagic перейдите в **Code signing**
2. Загрузите:
   - Distribution Certificate (.p12)
   - Provisioning Profile

### 4. Запуск сборки

1. Выберите workflow **ios-workflow**
2. Нажмите **Start new build**
3. Дождитесь завершения сборки
4. IPA будет автоматически загружен в TestFlight

## ⚙️ Конфигурация проекта

### Изменение Bundle Identifier

В `project.pbxproj` найдите и измените:

```
PRODUCT_BUNDLE_IDENTIFIER = com.monobank.clone;
```

На ваш уникальный идентификатор:

```
PRODUCT_BUNDLE_IDENTIFIER = com.yourcompany.yourapp;
```

### Изменение версии

В `project.pbxproj`:

```
MARKETING_VERSION = 1.0;
CURRENT_PROJECT_VERSION = 1;
```

### Изменение минимальной версии iOS

В `project.pbxproj`:

```
IPHONEOS_DEPLOYMENT_TARGET = 15.0;
```

## 🎨 Кастомизация

### Изменение цветовой схемы

В файлах View измените градиенты:

```swift
LinearGradient(
    gradient: Gradient(colors: [
        Color(red: 0.25, green: 0.27, blue: 0.65),  // Ваш цвет
        Color(red: 0.35, green: 0.25, blue: 0.65)   // Ваш цвет
    ]),
    startPoint: .top,
    endPoint: .bottom
)
```

### Добавление иконки приложения

1. Создайте иконку 1024x1024px
2. Перетащите в `Assets.xcassets/AppIcon.appiconset/`
3. Xcode автоматически создаст все размеры

### Изменение данных

Отредактируйте `Models.swift`:

```swift
extension Card {
    static let sampleCards: [Card] = [
        // Ваши данные карт
    ]
}
```

## 🐛 Решение проблем

### Ошибка "No such module"

```bash
# Очистите build
⌘ + Shift + K

# Очистите DerivedData
rm -rf ~/Library/Developer/Xcode/DerivedData

# Пересоберите проект
⌘ + B
```

### Ошибка подписи кода

1. Проверьте, что выбрана правильная Team
2. Убедитесь, что Bundle Identifier уникален
3. Проверьте срок действия сертификатов

### Приложение не запускается на устройстве

1. Проверьте версию iOS (должна быть 15.0+)
2. Убедитесь, что доверили разработчику в настройках
3. Проверьте, что устройство разблокировано

### Проблемы с Codemagic

1. Проверьте `codemagic.yaml` на синтаксические ошибки
2. Убедитесь, что все сертификаты загружены
3. Проверьте логи сборки

## 📊 Структура файлов

```
monobank/
├── MonobankClone/
│   ├── MonobankClone/
│   │   ├── MonobankCloneApp.swift
│   │   ├── ContentView.swift
│   │   ├── Models.swift
│   │   ├── Extensions.swift
│   │   ├── Views/
│   │   │   ├── MainView.swift
│   │   │   ├── CardView.swift
│   │   │   ├── CardDetailView.swift
│   │   │   ├── TransactionListView.swift
│   │   │   ├── BottomTabBar.swift
│   │   │   ├── ExchangeRatesView.swift
│   │   │   ├── ServicesView.swift
│   │   │   ├── CreditView.swift
│   │   │   ├── SavingsView.swift
│   │   │   └── MarketView.swift
│   │   └── Assets.xcassets/
│   └── MonobankClone.xcodeproj/
├── codemagic.yaml
├── README.md
├── SETUP.md
└── .gitignore
```

## 🔐 Безопасность

⚠️ **Важно**:

- Не коммитьте сертификаты в Git
- Используйте `.gitignore` для исключения приватных ключей
- Храните API ключи в переменных окружения
- Не используйте реальные данные карт

## 📚 Дополнительные ресурсы

- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui/)
- [Xcode Help](https://developer.apple.com/documentation/xcode)
- [Codemagic Docs](https://docs.codemagic.io/)
- [App Store Connect](https://developer.apple.com/app-store-connect/)

## 💡 Советы

1. **Используйте Preview** - SwiftUI Preview позволяет видеть изменения в реальном времени
2. **Горячие клавиши** - ⌘R (Run), ⌘B (Build), ⌘. (Stop)
3. **Симуляторы** - Тестируйте на разных размерах экранов
4. **Breakpoints** - Используйте для отладки
5. **Instruments** - Профилируйте производительность

## 🤝 Поддержка

Если возникли проблемы:

1. Проверьте этот документ
2. Посмотрите Issues в репозитории
3. Создайте новый Issue с описанием проблемы

---

**Удачи в разработке! 🚀**
